import Foundation

/// Master switch for feeding resource widgets (CPU, RAM, battery) random data
/// instead of real readings, for inspecting their visuals/animations. Toggle
/// here; leave false for normal use.
let widgetDebugRandom = false
